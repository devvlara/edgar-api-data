# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=market_intelligence_engine
DB_SCHEMA=edgar
DB_USER=dev
DB_PASSWORD=password

# SEC EDGAR — required header for all API requests (use your real name + email)
SEC_USER_AGENT=albert hues your@email.com
